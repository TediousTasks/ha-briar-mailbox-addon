name: "Briar Mailbox"
slug: "briar_mailbox"
version: "0.2.33"
description: "Briar Mailbox Server CLI packaged as a Home Assistant App (add-on)."
url: "https://code.briarproject.org/briar/briar-mailbox"

arch:
  - amd64

startup: services
init: false

ingress: true
ingress_port: 8080
ingress_entry: /

panel_icon: mdi:qrcode

# Do NOT publish ports externally
ports: {}
