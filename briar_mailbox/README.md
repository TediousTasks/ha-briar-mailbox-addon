# Briar Mailbox Home Assistant Add-on

This repository provides a Home Assistant app (add-on) for running the
Briar Mailbox server.

Add this repository via:
Settings → Apps → Install App → Repositories
